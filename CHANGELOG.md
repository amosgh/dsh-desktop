# Changelog

本项目的版本更新与用户可见修改记录在此文件中。

## [0.0.2] - 2026-08-16

### 修复

- 修复 macOS 主窗口关闭后，从 Dock 再次激活应用却无法恢复窗口的问题。
- 补回 macOS 原生“编辑”菜单，使输入框支持 `Cmd+V` 粘贴，以及撤销、剪切、复制和全选等标准操作。
- 修复“测试连接”错误调用 Harness 未注册的 `llm.discoverModels` 接口而显示失败的问题。
- “测试连接”现在直接验证模型服务的 `/models` 端点与 API Key，不产生对话 Token 费用。
- 为超时、无效凭证、错误端点、限流和异常响应提供明确的中文提示。
- 修复任务创建 RPC 失败后遗留 worktree、分支和数据库记录的问题，并对已创建的空会话执行尽力归档。
- 修复应用重启后已丢弃或缺失 worktree 的历史任务从项目列表消失的问题。
- 修复网关握手依赖日志变化重试、通知状态永不清理，以及审阅读取可与 worktree 删除并发的问题。
- 修复诊断导出包含自定义模型端点、外部编辑器失败静默、畸形导航 URL 和 sidecar 启动计时器未释放的问题。
- 对话中输出的 Markdown 报告链接现在可直接在 Codex 式右侧面板打开，支持任务 worktree 内的相对路径、绝对路径和 `file://` 链接。
- DeepSeek Harness 窗口中的 `Produced` Markdown 文件点击现在会在当前 Harness 窗口右侧打开，不再交给 macOS 默认应用。
- HTTP/HTTPS 链接改为在同一右侧面板的隔离浏览器中打开，并提供后退、前进、刷新和关闭操作，不再跳转到独立窗口或其他应用。
- 补齐 macOS 应用菜单中的“设置…”入口，确保 `Cmd+,` 可稳定打开设置页。
- 默认模型支持输入兼容网关返回的自定义模型 ID，并明确 `/models` 测试不代表完整对话协议兼容。

### 验证

- TypeScript 主进程与渲染进程类型检查通过。
- 13 个测试文件、29 项单元测试全部通过，覆盖模型连接、浏览地址策略、Harness `host.openPath` 拦截、Markdown 安全渲染与工作区/符号链接越界、诊断端点排除、Git/worktree、协议客户端回滚与 sidecar 生命周期。
- Harness 协议与自动恢复冒烟测试通过。
- Apple Silicon 本地应用重新打包并验证可启动、可恢复窗口。

### 发布说明

- 本版本发布源码与版本说明。
- 完善 GitHub 源码安装、环境检查、首次配置、验证、打包和常见问题说明。
- 明确要求 macOS 14+、Apple Silicon、原生 arm64 Node.js 24+，并在打包配置中声明最低系统版本。
- 修复干净 `npm ci` 后缺少 Electron runtime、导致开发启动和打包失败的问题。
- 未附带未经 Developer ID 签名和 Apple 公证的 macOS 安装包。

## [0.0.1] - 2026-08-16

### 首次发布

- 提供 DSH Desktop 的 Apple Silicon MVP 源码。
- 集成本地 DeepSeek Harness sidecar、项目管理、任务会话、审批、收件箱、设置与诊断功能。
